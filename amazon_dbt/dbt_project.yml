name: 'amazon_dbt'
version: '1.0.0'
config-version: 2

profile: 'amazon_dbt'

model-paths: ["models"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  amazon_dbt:
    staging:
      +materialized: view
    marts:
      +materialized: table
